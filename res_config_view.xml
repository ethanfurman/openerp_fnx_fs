<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record id="view_file_system_config_settings" model="ir.ui.view">
            <field name="name">Fnx File System Integration</field>
            <field name="model">file_system.config.settings</field>
            <field name="arch" type="xml">
                <form string="Configure fnx_fs" version="7.0" class="oe_form_configuration">
                    <header>
                        <button string="Apply" type="object" name="execute" class="oe_highlight"/>
                        or
                        <button string="Cancel" type="object" name="cancel" class="oe_link"/>
                    </header>

                    <group groups="base.group_multi_company">
                        <div>
                            <label for="company_id" string="Select Company"/>
                            <field name="company_id"
                                widget="selection"
                                on_change="onchange_company_id(company_id, context)"
                                class="oe_inline"/>
                        </div>
                    </group>

                    <group col="4">
                        <div>
                            <group>
                                <field name="prefix" placeholder="e.g. /mnt/users"/>
                                <field name="pattern" placeholder="regex for folders in `prefix`"/>
                            </group>
                        </div> 
                    </group>
                </form>
            </field>
        </record>

        <record id="action_file_system_config" model="ir.actions.act_window">
            <field name="name">Configure File System Integration</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">file_system.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
        </record>

        <menuitem id="menu_file_system_config" name="Fnx FS" parent="base.menu_config"
            sequence="14" action="action_file_system_config"/>

    </data>
</openerp>
